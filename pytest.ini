[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --tb=short
    --strict-markers
    -ra
markers =
    integration: Integration tests
    e2e: End-to-end tests
    slow: Slow tests
    gui: GUI tests
